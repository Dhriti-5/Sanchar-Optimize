# Minimal Lambda requirements - excludes heavy ML dependencies for faster deployment
# Use this for initial deployment, add ML layer separately if needed

# Core Framework
fastapi==0.109.0
pydantic==2.5.3
pydantic-settings==2.1.0
mangum==0.17.0  # ASGI adapter for AWS Lambda

# AWS Services
boto3==1.34.34

# HTTP & Networking
httpx==0.26.0

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.6

# Monitoring & Logging
loguru==0.7.2
